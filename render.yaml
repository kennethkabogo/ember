services:
  - type: web
    name: unifire
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ETHEREUM_RPC_URL
        sync: false
